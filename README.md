# google-automation
Automates Google search tasks using Python and Selenium. The bot performs searches, scrolls through results dynamically, collects relevant links from search results, removes duplicates, and saves them into a CSV file. Ideal for automating repetitive browsing and link extraction tasks.
